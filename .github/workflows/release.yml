name: Release

on:
  push:
    tags: ["v*"]

jobs:
  release:
    name: Trigger release
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1

      - name: Get yarn cache
        id: yarn-cache
        run: echo "::set-output name=dir::$(yarn cache dir)"

      - uses: actions/cache@v1
        with:
          path: ${{ steps.yarn-cache.outputs.dir }}
          key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
          restore-keys: |
            ${{ runner.os }}-yarn-

      - name: Build actions
        run: |
          yarn bootstrap
          yarn build
          yarn bundle

      - name: Generate the changelog
        id: changelog
        uses: ./.github/actions/generate-changelog@master
        env:
          GITHUB_AUTH: ${{ secrets.GITHUB_TOKEN }}

      - name: Commit CHANGELOG.md
        if: steps.changelog.outputs.updated == 1
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add CHANGELOG.md
          git commit -m "Update CHANGELOG.md [skip ci]" --no-verify --quiet

      - name: Push CHANGELOG.md
        if: steps.changelog.outputs.updated == 1
        uses: ad-m/github-push-action@v0.5.0
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          branch: 'master'
          force: false

      - name: Create a GitHub release
        uses: ./.github/actions/publish-github-release@master
        env:
          GITHUB_AUTH: ${{ secrets.GITHUB_TOKEN }}
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Create an NPM release
        run: |
          echo "//registry.npmjs.org/:_authToken=${NPM_TOKEN}" > ~/.npmrc
          yarn ci:npm-publish
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
