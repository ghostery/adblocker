{
  "name": "adblocker",
  "version": "1.0.0",
  "description": "Cliqz adblocker library",
  "main": "index.js",
  "repository": "https://www.github.com/cliqz-oss/adblocker",
  "author": "Cliqz",
  "license": "MPL-2.0",
  "scripts": {
    "clean": "rm -rf build",
    "lint": "tslint -c tslint.json 'src/**/*.ts'",
    "build": "npm run clean && mkdir build && tsc -p .",
    "watch": "tsc -p . --watch",
    "pretest": "npm run lint",
    "test": "npm run build && jest --coverage build/test/",
    "dev": "npm run build && concurrently 'npm run watch' 'jest build/test/ --watch'"
  },
  "jest": {
    "testMatch": [
      "**/test/**/*.test.js"
    ],
    "testEnvironment": "node"
  },
  "devDependencies": {
    "@types/jest": "21.1.8",
    "concurrently": "3.5.1",
    "jest": "21.2.1",
    "rollup": "^0.52.1",
    "tslint": "5.8.0",
    "typescript": "2.6.2"
  },
  "dependencies": {
    "@types/text-encoding": "0.0.32",
    "fnv-plus": "git+https://git@github.com/tjwebb/fnv-plus.git",
    "text-encoding": "0.6.4",
    "tldjs": "2.2.0"
  }
}
